import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
  title: 'Ozz Wide Solutions - Australian Electrical Solutions Provider',
  description: 'Professional electrical services across Australia. Commercial, residential, and industrial solutions with 24/7 emergency support. CEC accredited and fully licensed.',
  keywords: 'electrical services Australia, commercial electrician, residential electrical, industrial electrical, emergency electrician, solar installation, electrical maintenance',
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <head>
        <style>{`
html {
  font-family: ${GeistSans.style.fontFamily};
  --font-sans: ${GeistSans.variable};
  --font-mono: ${GeistMono.variable};
}
        `}</style>
      </head>
      <body>{children}</body>
    </html>
  )
}
